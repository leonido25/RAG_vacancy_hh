# Простой HR RAG Assistant 🤖 для поиска вакансий с сайта hh.ru
## 📋 Описание

Этот проект представляет собой систему поиска вакансий, которая:
- Ищет релевантные вакансии в базе данных
- Генерирует структурированные ответы на русском языке
- Показывает источники информации для каждого ответа
- Имеет удобный веб-интерфейс

## 🛠 Технологии

- **Frontend**: Streamlit
- **LLM**: Ollama (модель llama3)
- **Векторная база**: FAISS
- **Эмбеддинги**: sentence-transformers/paraphrase-multilingual-mpnet-base-v2
- **Фреймворк**: LangChain

## 📁 Структура проекта
├── app.py # Streamlit веб-приложение

├── hh_vacancies_data_final.csv # собранный датасет с данными вакансий

├── rag_pipeline.py # Основная и финальная RAG логика

├── rag_project_parsing.ipynb # основной ноутбук с парсингом вакансий

├── rag_project_retrieval (1).ipynb # ноутбук с проверкой разным методов чанкования и разной rag логикой

└── README.md

## Компоненты проекта

### **Парсинг данных**
- Парсер извлекает всевозможные данные связанные с вакансией: зарплата, требования, обязанности, описание и т.д
- далее я формирую датафрейм, обрабатываю полные описания 'description_cleaned'(Удаление HTML, Удаление URL, приведение к нижнему регистру и т.д)
- формирую meta header, включающий в себя все основные данные о вакансии и сохраняю итоговый датасет

### **chunking и rag цепочка**
- Сначала я извлёк данные из датасета при помощи функции "create_general_chunks" и помеситил их в плоский список
- Протестировал Recursive chunking и Semantic chunking. Первый метод сплитит данные по приоритету разделителей: "\n\n" (абзацы), "\n" Одинарный перевод строки
- Второй метод(Semantic chunking) использует векторную схожесть и порог: breakpoint_threshold_amount=85
- взял классическую цепочку: Поиск контекста (вопрос -> retriever -> форматирование) -> Применение промпта к контексту и вопросу -> Передача в модель llm ->  Получение ответа в виде строки
- Также применил Multi Query для создания 5 вопросов, похожих на исходный для извлечения дополнительных полезных данных из базы данных.

